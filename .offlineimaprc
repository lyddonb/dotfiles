[general]
ui=ttyui
accounts = BeauGmail, RealKinetic
pythonfile=~/.mutt/offlineimap.py
fsync = False

[Account BeauGmail]
localrepository = BeauGmail-Local
remoterepository = BeauGmail-Remote
status_backend = sqlite
postsynchook = notmuch new

[Repository BeauGmail-Local]
type = Maildir
localfolders = ~/.mail/lyddonb
nametrans = lambda folder: { 'flagged': 'Starred',
                            'archive': 'All Mail',
                           }.get(folder, folder)

[Repository BeauGmail-Remote]
maxconnections = 1
type = Gmail
remoteuser = lyddonb@gmail.com
remotepasseval = get_keychain_pass(account="lyddonb@gmail.com", server="imap.gmail.com")
cert_fingerprint=6d1b5b5ee0180ab493b71d3b94534b5ab937d042
readdelete = no
nametrans = lambda folder: { 'flagged': 'Starred',
                            'archive': 'All Mail',
                           }.get(folder, folder)

[Account RealKinetic]
localrepository = RealKinetic-Local
remoterepository = RealKinetic-Remote
status_backend = sqlite
postsynchook = notmuch new

[Repository RealKinetic-Local]
type = Maildir
localfolders = ~/.mail/realkinetic
nametrans = lambda folder: { 'flagged': 'Starred',
                            'archive': 'All Mail',
                           }.get(folder, folder)

[Repository RealKinetic-Remote]
maxconnections = 1
type = Gmail
remoteuser = beau.lyddon@rkinetic.com
remotepasseval = get_keychain_pass(account="beau.lyddon@rkinetic.com", server="imap.gmail.com")
cert_fingerprint=6d1b5b5ee0180ab493b71d3b94534b5ab937d042
readdelete = no
nametrans = lambda folder: { 'flagged': 'Starred',
                            'archive': 'All Mail',
                           }.get(folder, folder)
